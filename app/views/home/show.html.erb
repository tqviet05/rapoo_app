<% provide(:title, 'Profile') %>
<section class="container">
  <div class="row mt-2">
    <div class="col-3">

      <%# SIDE BAR %>
      <%= render partial: 'home/side_bar_user.html.erb' %>
      <%# --/SIDE BAR %>
    </div> 
    <div class="col-9">
      <h5> My profile </h5>
      <hr>

      <p> Name: <%=current_user.name ? current_user.name : "No information"%></p>
      <p> Email: <%=current_user.email ? current_user.email : "No information" %></p>
      <p> Phone: <%=current_user.phone ? current_user.phone : "No information" %></p>
      <p> Address: <%=current_user.address ? current_user.address : "No information" %></p>
      <p> Gender: <%=current_user.gender ? "Male" : "Female" %></p>
      <p> Birthday: <%=current_user.birthday ? current_user.birthday : "No information" %></p>
      <%= link_to "Update Profile", edit_home_path(current_user.id), class: 'btn btn-dark' %>
      <%# <%= form_with model:deliveries_form, url: order_index_path, remote: false, id: 'js-form-payment' do |f| %>  
      <%# <%= f.text_field :name, class: "form-control"  %> 

      <%# <%= form_with current_user %> 
      <%# <%end %> 
    </div>
  </div>



</section>