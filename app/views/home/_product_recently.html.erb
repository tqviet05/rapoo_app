<div class="col-md-2">
  <div class="card p-3 text-center card-height">
    <%= link_to image_tag("product#{rand(2...4)}.jpeg", alt:"", width:"170px"), product_path(p)   %>
    <p class="h5 pt-2"><%= p.name %> </p>
  </div>
</div>
